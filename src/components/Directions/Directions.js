import React from "react";
import "./Directions.css";

const Directions = props => (

    <div className="jumbotron jumbotron-fluid">
        <h1>
            Trying and click on every charater only once!
        </h1>
    </div>

);
export default Directions;